import { AnimarAoEntrar } from './animar-ao-entrar';

describe('AnimarAoEntrar', () => {
  it('should create an instance', () => {
    const directive = new AnimarAoEntrar();
    expect(directive).toBeTruthy();
  });
});
